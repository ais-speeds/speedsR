# speedsR
R data package to host a repository of sports-specific datasets for the AIS 'SPEEDS' project.
This package can be downloaded from the AIS SPEEDS GitHub repository and loaded in your IDE as a regular library.
